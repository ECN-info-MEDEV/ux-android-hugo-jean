<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AffluNantes" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Colors. -->
        <item name="colorPrimary">@color/blue_dark</item>
        <item name="colorPrimaryVariant">@color/blue</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="backgroundColor">@color/blue_light</item>
        <item name="colorOnBackground">@color/black_blue</item>

        <!-- Parameters. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimary</item>
        <item name="android:navigationBarColor" tools:targetApi="l">?attr/colorPrimary</item>
        <item name="android:colorBackground" tools:targetApi="l">?attr/backgroundColor</item>
        <item name="android:textColor" tools:targetApi="l">?attr/colorOnBackground</item>
    </style>
</resources>
